export type { InlineOptions, Middleware, Placement, Strategy } from "@floating-ui/dom";
export { autoPlacement, flip, hide, inline, offset, shift, size } from "@floating-ui/dom";
export { arrow } from "./arrow";
