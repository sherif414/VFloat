Status: completed

# Align API docs and examples with the chosen contract

## Parent

`C:\projects\VFloat\.scratch\list-navigation-hardening\PRD.md`

## What to build

Update the `useListNavigation` API documentation and examples so they describe the chosen public contract accurately. The docs should explain the collection-backed model if that contract is confirmed, preserve the boundary between list navigation and role management, and remove stale options or examples that no longer compile conceptually.

## Acceptance criteria

- [x] The documented `UseListNavigationOptions` shape matches the exported TypeScript interface.
- [x] The main example uses the chosen contract and can be followed without relying on stale DOM/index options.
- [x] The docs explain that list navigation moves active item state and role management owns ARIA semantics.
- [x] References to unsupported options are removed or clearly replaced by supported collection-backed equivalents.
- [x] Documentation remains discoverable from the existing API index/sidebar structure if those pages currently link to `useListNavigation`.
- [x] Docs validation is run with the repo's standard docs command where practical.

## Blocked by

- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\01-confirm-collection-backed-contract.md`
- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\02-guard-closed-floating-navigation.md`
- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\03-harden-branch-expansion-targets.md`
- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\04-reconcile-list-navigation-strategies.md`

## Comments

> _This was generated by AI during triage._

## Agent Brief

**Category:** enhancement
**Summary:** Align the `useListNavigation` API documentation and guides with the collection-backed model

**Current behavior:**
The documentation for `useListNavigation` (located at `docs/api/use-list-navigation.md` and related guides) might still contain outdated references to the DOM-index-based API parameters (e.g. `listRef`, `activeIndex`, `onNavigate`), which does not align with the current collection-backed implementation contract.

**Desired behavior:**
Rewrite the `useListNavigation` API documentation file `docs/api/use-list-navigation.md` and any other affected pages to accurately describe the collection-backed model. Clearly document `collection` option, and provide complete, compilation-ready code examples showcasing how to define a collection with `useCollection` and pass it to `useListNavigation`. Explicitly detail that list navigation manages keyboard events and active item movement, while role management handles ARIA attributes and DOM attributes.

**Key interfaces:**

- `docs/api/use-list-navigation.md` — main documentation page.
- Code examples in the docs — must use the real, collection-backed API.

**Acceptance criteria:**

- [x] `docs/api/use-list-navigation.md` correctly describes `UseListNavigationOptions` options matching the actual interface in `use-list-navigation.ts`.
- [x] Stale options such as `listRef`, `activeIndex`, and `onNavigate` are entirely removed or replaced.
- [x] Examples are fully updated to use `useCollection` and pass it into `useListNavigation`.
- [x] The text clearly delineates the separation of concerns: list navigation owns active-item keyboard coordination, and role management owns ARIA properties.
- [x] No broken links or formatting errors in the updated documentation files.

**Out of scope:**

- Redesigning the VitePress documentation theme, sidebars, or other unrelated API pages.
