Status: completed

# Add focused regression coverage for hardened behavior

## Parent

`C:\projects\VFloat\.scratch\list-navigation-hardening\PRD.md`

## What to build

Add behavior-first regression coverage for the hardened `useListNavigation` and `useCollection` behavior. The suite should cover the externally visible outcomes that matter to users and maintainers without locking tests to private implementation details.

## Acceptance criteria

- [x] Tests cover mounted-but-closed floating elements receiving navigation keydown events.
- [x] Tests cover branch expansion with disabled immediate children.
- [x] Tests cover branch expansion where every descendant is disabled.
- [x] Tests cover RTL nested expand/collapse behavior after the hardening work.
- [x] Tests cover dynamic collection updates recovering from stale active values where that behavior is part of the chosen contract.
- [x] Tests cover any final strategy-module decision through public behavior rather than private helper internals.
- [x] Focused test commands are documented in the issue comments or implementation notes when the issue is completed.

## Blocked by

- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\02-guard-closed-floating-navigation.md`
- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\03-harden-branch-expansion-targets.md`
- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\04-reconcile-list-navigation-strategies.md`

## Comments

> _This was generated by AI during triage._

## Agent Brief

**Category:** enhancement
**Summary:** Add behavior-first regression test coverage for the hardened keyboard navigation and branch expansion behaviors

**Current behavior:**
The existing test suites in `use-list-navigation.test.ts` and `use-collection.test.ts` lack assertions for the specific edge cases covered under this hardening effort, such as keyboard input on closed mounted elements, branch expansion with disabled children, and detailed RTL expand/collapse outcomes.

**Desired behavior:**
Add focused regression unit tests in `src/composables/list-navigation/use-list-navigation.test.ts` and `src/composables/collection/use-collection.test.ts` that specifically verify the new resilient behaviors. The tests should dispatch simulated keyboard events (or interact with collection return methods) and assert the expected updates to `activeValue`, `expandedValues`, `open` state, and default-prevention outcomes.

**Key interfaces:**

- `src/composables/list-navigation/use-list-navigation.test.ts` — to add integration-level keyboard test cases.
- `src/composables/collection/use-collection.test.ts` — to add unit/collection-level traversal test cases.

**Acceptance criteria:**

- [x] Tests verify that keydowns on a mounted floating element are ignored when `open` is false.
- [x] Tests verify that branch expansion correctly targets the first enabled descendant, skips disabled immediate children, and stays on the opener if all descendants are disabled.
- [x] Tests verify that RTL layout reverses expansion and collapse keys (ArrowLeft to expand, ArrowRight to collapse) correctly.
- [x] Tests verify that Tab key press sets the open state to false but does NOT call `e.preventDefault()`.
- [x] Stale strategy tests are completely cleaned up or removed (coordinated with Issue 04).
- [x] The full test suite runs and passes cleanly via Vite+ using `vp test`.

**Out of scope:**

- Testing UI rendering or styling.
- Adding browser-based E2E tests (unit and integration level in Vitest is sufficient).
