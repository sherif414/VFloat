Status: completed

# Reconcile list-navigation strategy helpers

## Parent

`C:\projects\VFloat\.scratch\list-navigation-hardening\PRD.md`

## What to build

Decide whether the existing list-navigation strategy helpers should be wired into `useListNavigation` as the deep keyboard-navigation module, or removed if they no longer represent shipped behavior. The goal is to prevent tests from covering unreachable behavior and to keep strategy behavior aligned with the public composable.

## Acceptance criteria

- [x] A decision is recorded: retain and wire strategies into `useListNavigation`, or remove stale strategy code/tests.
- [x] If retained, strategy behavior is reachable through the public composable and covered by integration tests.
- [x] If removed, stale tests and exports that describe unshipped behavior are deleted or rewritten.
- [x] Horizontal, RTL, and grid/navigation behavior are represented only where they are part of the chosen public contract.
- [x] Tests assert externally observable behavior rather than private helper selection.

## Blocked by

- `C:\projects\VFloat\.scratch\list-navigation-hardening\issues\01-confirm-collection-backed-contract.md`

## Comments

> _This was generated by AI during triage._

## Agent Brief

**Category:** enhancement
**Summary:** Remove obsolete, index-based list navigation strategy files and their stale tests

**Current behavior:**
The files `src/composables/list-navigation/strategies.ts` and `src/composables/list-navigation/list-navigation-strategies.test.ts` define and test index-based navigation strategy classes (`VerticalNavigationStrategy`, `HorizontalNavigationStrategy`, `GridNavigationStrategy`). These files are completely unused by the `useCollection`-backed `useListNavigation` composable, which implements its own orientation handling directly using the data-first collection methods.

**Desired behavior:**
Formally delete the unused and obsolete files `src/composables/list-navigation/strategies.ts` and `src/composables/list-navigation/list-navigation-strategies.test.ts` from the repository to clean up dead code and avoid false-confidence test coverage on unshipped behaviors.
Ensure that the codebase still runs cleanly, that all valid tests in `use-list-navigation.test.ts` pass, and that the public orientation contracts (vertical, horizontal, and grid) remain supported via the primary collection/composable flow.

**Key interfaces:**

- `strategies.ts` — to be deleted.
- `list-navigation-strategies.test.ts` — to be deleted.

**Acceptance criteria:**

- [x] `src/composables/list-navigation/strategies.ts` is deleted from the filesystem.
- [x] `src/composables/list-navigation/list-navigation-strategies.test.ts` is deleted from the filesystem.
- [x] Clean build and run (`vp check` and `vp test` pass successfully with no errors or orphaned imports/references).
- [x] No public APIs/types exposed in `use-list-navigation.ts` or `use-collection.ts` are broken.

**Out of scope:**

- Restoring index-based orientation strategies.
- Modifying non-stale navigation/collection logic.
