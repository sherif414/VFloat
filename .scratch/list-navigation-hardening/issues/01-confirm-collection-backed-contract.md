Status: completed

# Confirm collection-backed `useListNavigation` contract

## Parent

`C:\projects\VFloat\.scratch\list-navigation-hardening\PRD.md`

## What to build

Decide whether the current `useCollection`-backed `useListNavigation` API is the canonical public contract, or whether the older DOM/index-based API should be restored before hardening work proceeds.

This is a human-in-the-loop slice because the decision determines whether future implementation issues should align docs and tests to the collection contract or preserve the older documented API surface.

## Acceptance criteria

- [x] The chosen public contract for `useListNavigation` is explicitly recorded in this issue.
- [x] The decision states whether `collection` is required or whether DOM/index options such as `listRef`, `activeIndex`, and `onNavigate` should remain public.
- [x] The decision confirms that list navigation owns active-item movement while role management owns ARIA semantics.
- [x] Follow-up issues can proceed without re-litigating the API direction.

## Blocked by

None - can start immediately

## Comments

> _This was generated by AI during triage._

## Agent Brief

**Category:** enhancement
**Summary:** Establish the collection-backed model as the canonical public contract for `useListNavigation`

**Current behavior:**
The `useListNavigation` composable accepts a `collection` option of type `UseCollectionReturn<any>`. However, there is some uncertainty or lack of explicit documentation confirming that this collection-backed model is the sole canonical contract, and whether any old index/DOM-based fallbacks are required.

**Desired behavior:**
Formally choose and document the `useCollection`-backed API as the canonical public contract for `useListNavigation`. Ensure that all other hardening issues proceed with this model as the explicit and only supported contract. Deprecate and remove any remaining references to DOM/index-based configuration like `listRef`, `activeIndex`, or `onNavigate` from the public design.

**Key interfaces:**

- `UseListNavigationOptions` in `use-list-navigation.ts` — maintains the `collection: UseCollectionReturn<any>` shape.
- `useListNavigation()` signature — takes `(context, options)` as its public contract.

**Acceptance criteria:**

- [x] The collection-backed contract for `useListNavigation` is formally recorded in this issue and in the repository's architecture baseline.
- [x] Confirmed that DOM/index options (`listRef`, `activeIndex`, `onNavigate`) are officially removed/deprecated and not supported.
- [x] Converted list navigation's scope to strictly focus on keyboard-driven collection state transitions, leaving ARIA semantics to role management.

**Out of scope:**

- Restoring index-based properties or fallback states to `useListNavigation`.
